import DepartmentTemplate from "./DepartmentTemplate";

export default function MEEPage() {
  // Add department-specific sections here and pass them as extraSections.
  // Example:
  // const extraSections = <section id="MEE-custom">...</section>;
  // return <DepartmentTemplate code="MEE" extraSections={extraSections} />;
  return <DepartmentTemplate code="MEE" />;
}
