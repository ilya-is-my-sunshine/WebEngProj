import DepartmentTemplate from "./DepartmentTemplate";

export default function CPEPage() {
  // Add department-specific sections here and pass them as extraSections.
  // Example:
  // const extraSections = <section id="CPE-custom">...</section>;
  // return <DepartmentTemplate code="CPE" extraSections={extraSections} />;
  return <DepartmentTemplate code="CPE" />;
}
