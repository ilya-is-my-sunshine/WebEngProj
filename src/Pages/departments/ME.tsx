import DepartmentTemplate from "./DepartmentTemplate";

export default function MEPage() {
  // Add department-specific sections here and pass them as extraSections.
  // Example:
  // const extraSections = <section id="ME-custom">...</section>;
  // return <DepartmentTemplate code="ME" extraSections={extraSections} />;
  return <DepartmentTemplate code="ME" />;
}
