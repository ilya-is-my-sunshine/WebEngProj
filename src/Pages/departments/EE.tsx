import DepartmentTemplate from "./DepartmentTemplate";

export default function EEPage() {
  // Add department-specific sections here and pass them as extraSections.
  // Example:
  // const extraSections = <section id="EE-custom">...</section>;
  // return <DepartmentTemplate code="EE" extraSections={extraSections} />;
  return <DepartmentTemplate code="EE" />;
}
