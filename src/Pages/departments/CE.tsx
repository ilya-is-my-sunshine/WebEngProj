import DepartmentTemplate from "./DepartmentTemplate";

export default function CEPage() {
  // Add department-specific sections here and pass them as extraSections.
  // Example:
  // const extraSections = <section id="CE-custom">...</section>;
  // return <DepartmentTemplate code="CE" extraSections={extraSections} />;
  return <DepartmentTemplate code="CE" />;
}
