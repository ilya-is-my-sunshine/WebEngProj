import DepartmentTemplate from "./DepartmentTemplate";

export default function IEPage() {
  // Add department-specific sections here and pass them as extraSections.
  // Example:
  // const extraSections = <section id="IE-custom">...</section>;
  // return <DepartmentTemplate code="IE" extraSections={extraSections} />;
  return <DepartmentTemplate code="IE" />;
}
