import DepartmentTemplate from "./DepartmentTemplate";

export default function ECEPage() {
  // Add department-specific sections here and pass them as extraSections.
  // Example:
  // const extraSections = <section id="ECE-custom">...</section>;
  // return <DepartmentTemplate code="ECE" extraSections={extraSections} />;
  return <DepartmentTemplate code="ECE" />;
}
